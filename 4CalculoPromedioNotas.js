console.log("Programa que calcula promedio de nota")

function calcularPromedio(notas){
    let notasUsuario = [];
    
    notasUsuario = parseFloat(prompt("Ingrese las notas separadas por coma: "));
    for (for i=0; i<)
}